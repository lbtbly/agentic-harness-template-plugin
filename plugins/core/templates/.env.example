# ============================================================
# Environment variables — TEMPLATE (no real values)
# Copy to .env (gitignored) and fill in from the team vault.
# Procedure and rotation: docs/SECURITY.md
# ============================================================

# --- LLM / Claude (CI only: GitHub Actions secrets) ---
# ANTHROPIC_API_KEY=        # Do NOT set locally unless explicitly needed

# --- Third-party services (examples — adapt via /core:new-project) ---
# DATABASE_URL=             # postgres://user:pass@host:5432/db
# SENTRY_DSN=
# STRIPE_SECRET_KEY=        # sk_test_... in dev, NEVER sk_live_ locally
