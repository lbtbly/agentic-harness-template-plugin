{
  "$comment": "State-layer config (ADR-0007). backend: none | github-projects | gitlab | jira | notion (implemented, native semantics) | linear | trello (stubs). forge: where PRs/MRs live — feedback is ALWAYS pulled from the forge, independent of backend. Set by /core:new-project; the board outlives /orchestrator:enable-orchestrator and /orchestrator:disable-orchestrator.",
  "backend": "none",
  "forge": "none"
}
