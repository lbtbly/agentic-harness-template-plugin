# Egress allowlist — the SINGLE source of truth for unattended-run network
# access. Consumed by BOTH enforcers (keep them in parity — a test checks):
#   1. .devcontainer/init-firewall.sh   (default-deny iptables, the outer wall)
#   2. settings.orchestrator.json → sandbox.network.allowedDomains (native sandbox)
# Extend deliberately: every entry widens the blast radius.
api.anthropic.com
github.com
api.github.com
codeload.github.com
registry.npmjs.org
pypi.org
files.pythonhosted.org

# --- board backends: NOT enabled by default ----------------------------------
# A remote state backend pushes status from INSIDE the sandbox, so its host must
# be reachable or every `orch state push-*` fails closed mid-run. Uncomment the
# ONE you use (and mirror it into settings.orchestrator.json + init-firewall.sh —
# the parity test enforces that once it is uncommented).
# api.linear.app          # backend: linear
# api.notion.com          # backend: notion
# <your-site>.atlassian.net   # backend: jira — per-tenant, no fixed host
# gitlab.com                  # backend: gitlab — or your self-hosted host
