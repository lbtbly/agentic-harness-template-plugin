{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "orchestrator",
  "description": "The overnight autonomous EPIC loop: /enable-orchestrator (six verified preconditions), /kickoff (daily driver), /plan (DoD authoring + independent review), /run (run-to-completion with risk-gated auto-merge), /disable-orchestrator; planner + integration-checker agents; dod-verify + nightly-orchestrator workflows; runtime, risk-policy, CODEOWNERS and digest templates.",
  "version": "1.2.1",
  "author": {
    "name": "lambertbouley"
  },
  "homepage": "https://github.com/lambertstudi/agentic-harness-template-plugin",
  "repository": "https://github.com/lambertstudi/agentic-harness-template-plugin",
  "license": "MIT",
  "keywords": [
    "autonomous",
    "overnight",
    "orchestrator",
    "dod",
    "auto-merge"
  ],
  "dependencies": [
    {
      "name": "core",
      "version": ">=1.3.0"
    }
  ],
  "userConfig": {
    "board_token": {
      "type": "string",
      "title": "Board API token (Jira or Notion \u2014 the headless adapter lane)",
      "sensitive": true
    },
    "plugin_repo_url": {
      "type": "string",
      "title": "Marketplace repo URL the CI runner fetches the plugin from"
    }
  }
}
